# El sitio es HTML plano: no hay build, se publica la raiz tal cual.
[build]
  publish = "."
  functions = "netlify/functions"

[functions]
  node_bundler = "esbuild"

# La clave secreta de Mercado Pago NO va aca.
# Se configura en el panel de Netlify:
#   Site configuration -> Environment variables -> MP_ACCESS_TOKEN
